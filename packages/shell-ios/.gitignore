# ios/.gitignore (generated by Capacitor) already covers build output, the
# copied web assets and the generated config. This is only what it misses:
# per-developer signing, which must never be committed.
ios/Local.xcconfig
